[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "9.9.4"
  NODE_ENV = "development"
  NPM_FLAGS = "--no-audit --no-fund --include=dev --legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
